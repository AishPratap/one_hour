package chap2_linked_list;

import java.util.*;
/**
 *
 * @author hkhoi
 */
public class ToDo {
    
    /**
     * Write code to remove duplicates from an unsorted linked list.
     * How would you solve this problem if a temporary buffer is not allowed?
     * @param list 
     */
    public void removeDuplicated(LList list) {
        
    }
    
    /**
     * Implement an algorithm to find the kth 
     * to last element of a singly linked list.
     * @param list
     * @param k
     * @return 
     */
    public Node kthLastElement(LList list, int k) {
        
        return null;
    }
    
    /**
     * Implement an algorithm to delete a node in the middle of a singly linked list, given only access to that node
     * EXAMPLE
     * Input: the node c from the linked list a->b->c->d->e
     * Result: nothing is returned, but the new linked list looks like a- >b- >d->e
     * @param list
     * @param node 
     */
    public void deleteMiddle(LList list, Node node) {
        
    }
    
    /**
     * Write code to partition a linked list around a value x, such that all
     * nodes less than x come before all nodes greater than or equal to x.
     * @param list
     * @param val 
     */
    public void partition(LList list, int val) {
        
    }
    
    /**
     * Sum up two arrays of integers.
     * @param list1
     * @param list2
     * @return 
     */
    public LList add(LList list1, LList list2) {
        return null;
    }
    
    /**
     * Given a circular linked list, return the node where the loop begins
     * @param list
     * @return 
     */
    public Node getLoop(LList list) {
        
        return null;
    }
    
    
    /**
     * Given a linked list, return if it is palindrome
     * @param list
     * @return 
     */
    public boolean isPalindrome(LList list) {
        return false;
    }
}
