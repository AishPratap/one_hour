package chap5_bit;

import java.util.*;
/**
 *
 * @author hkhoi
 */
public class ToDo {
    
    /**
     * Input: N=10000000000,M=10011,i=2,j=6 Output:N = 10001001100
     * @param big
     * @param small
     * @return 
     */
    public int insertBit(int big, int small, int from, int to) {
        
        return 0;
    }
    
    /**
     * Given a double, return its binary representation (at most 32)
     * Give an error return if it exceeds 32 characters
     * @param num
     * @return 
     */
    public String binDouble(double num) {
        return null;
    }
    
    /**
     * Given two integers, return the number of bits to convert a to b
     * Example: 31, 14 -> 2
     * @param a
     * @param b
     * @return 
     */
    public int toConvert(int a, int b) {
        return 0;
    }
    
    /**
     * Write a program to swap odd and even bits in an integer with as
     * few instructions as possible (e.g., bit 0 and bit 1 are swapped, 
     * bit 2 and bit 3 are swapped, and soon).

     * @param a
     * @return 
     */
    public int swapOddEvenBit(int a) {
        
        return 0;
    }
}
