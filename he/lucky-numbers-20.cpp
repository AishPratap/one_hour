#include <stdio.h>
#include <ctype.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	freopen("_in", "r", stdin);
	freopen("_out", "w", stdout);

	int str;
	scanf("%d", &str);

	printf("%d\n", str);

	return 0;
}